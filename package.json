{
  "name": "pejebot",
  "version": "0.1.0",
  "description": "Discord bot that generates AI phrases and images",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@openrouter/sdk": "^0.5.1",
    "discord.js": "^14.16.3",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.7.3"
  }
}